# Connectors

Connector scope:
- GitHub App repo access for PR/comment tooling
- Optional Slack run status notifications
- CI trigger/result tooling

Rules:
- Expose connector capabilities as tools with strict schemas.
- Require explicit scope checks before external calls.
