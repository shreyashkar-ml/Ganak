# Option C Readiness

Stable interfaces required for future self-hosted runner mode:
- Event envelope schema
- Tool call/result schemas
- Runner job schema
- Sandbox proxy interface

Guidance:
- Add new behavior behind interfaces, not via cross-module coupling.
