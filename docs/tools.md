# Tools

Tool contract:
- Strict tool name
- Input schema
- Output schema
- Required scopes

Policy sources:
- `contracts/permissions/tool_scopes.yaml`
- Org-level allowlists

Guidance:
- Reject tool calls when scope or schema validation fails.
